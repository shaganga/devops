def util = load("${env.WORKSPACE}/scripts/build_util.groovy")
pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                script{
                    def util = load("${env.WORKSPACE}/scripts/build_util.groovy" 
                    util.test1()
   
                }
            }
        }
    }
}
