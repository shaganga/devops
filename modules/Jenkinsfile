Map modules = [:]
pipeline {
    agent any
    stages {
        stage('test') {
            steps {
                script{
                    modules.first = load "modules/first.groovy"
                    modules.first.test1()
   
                }
            }
        }
    }
}
